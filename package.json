{
  "name": "bean-to-cup",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "tsc -p tsconfig.test.json && node --test .test-build/tests/core.test.js"
  },
  "dependencies": { "gsap": "3.13.0" },
  "devDependencies": { "typescript": "5.8.3", "vite": "6.3.5", "@types/node": "22.15.30" }
}
